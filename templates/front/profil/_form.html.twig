{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="flex w-full justify-end">
        <button class="bg-[#65C986] py-2 px-4 rounded-md text-[#202020] font-bold my-10">{{ button_label|default('Save') }}</button>
    </div>
    
{{ form_end(form) }}
